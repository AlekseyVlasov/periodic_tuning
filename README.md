# periodic_tuning


**Install dependencies**:

```bash
conda create -n periodic_tuning python=3.11 -y
conda activate periodic_tuning
pip install -r requirements.txt
pip install --no-build-isolation mamba-ssm[causal-conv1d]
```
